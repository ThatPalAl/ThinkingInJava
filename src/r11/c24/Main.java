package r11.c24;

import java.util.PriorityQueue;

public class Main {
    public static void main(String[] args) {
        PriorityQueue<Template> templates = new PriorityQueue<>();
        Template template = new Template();
        templates.offer(template);
    }
}
